# Secrets — never commit. Real secrets live in HERMES_HOME/.env, never in this repo.
.env
*.env
# NOTE: per-distribution `.env.EXAMPLE` files under dist/ ARE committed (they contain no secrets).

# Hermes runtime/state artifacts (only if ever copied in)
auth.json
models.json
desktop.json
state.db*
response_store.db*
kanban.db*
sessions/
memories/
logs/
cache/
mcp-tokens/

# Local backups / archives produced during testing
*.bak
*.bak.*
*.zip
*.tar.gz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
# NOTE: `dist/` here is COMMITTED (compiled Hermes distributions), NOT a Python build dir — do not ignore it.

# Local agent / tooling state
.omo/
.codegraph/
.dev/
.kilo/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
