{
  "agent_instructions": "You are pointed at the hermes-setup repo. To set up a Hermes agent from it: (1) present the `profiles` list to the user — each entry's `name`, `kind`, `description`, and `apply_modes` (`kind: base` `general` is the recommended FREE, no-per-call-cost, general-purpose default; `general-pro` is the PAID Nous Portal variant — frontier models + the Nous Tool Gateway, requires a paid Portal subscription; `locale` is a region base; `persona` is specialized). A profile whose `apply_modes` lists `pro` can also be installed on the paid Portal base by adding the installer's `--pro` / `-Pro` flag (bootstrap uses `--portal` / `-Portal`); with no flag the free chain is used; (2) ask which `name` they want (and, if it supports `pro`, whether they want the free default or the paid Portal mode); (3) ask HOW to apply it — the two paths are different and the user should choose: (a) a NEW ISOLATED profile (recommended default) — its own sessions/auth, leaving the current profile untouched; run the profile's `local_posix_command` / `local_windows_command` / `standalone_*_command` (the `install.sh` / `install.ps1` scripts — always use these, NOT a bare `hermes profile install`, or `/finish-setup` will not register on Desktop/default surfaces); OR (b) EXTEND the CURRENT/default profile — inherit its existing sessions/auth/skills; run `bootstrap.sh --template <name>` / `bootstrap.ps1 -Template <name>` instead. Warn the user that a NEW isolated profile starts with empty session history and separate auth (their old history is intact in its original profile, reachable via `hermes -p <old>`); (4a) IF you are already running inside a local clone of this repo, run that profile's `local_posix_command` (macOS/Linux) or `local_windows_command` (Windows) — append `--pro` / `-Pro` for paid Portal mode — for a new profile, or the `bootstrap.*` command to extend the current one; (4b) OTHERWISE run its `standalone_posix_command` (macOS/Linux) or `standalone_windows_command` (Windows), first replacing <RAW_REPO_URL> and <REPO_GIT_URL> with the raw-content and git URLs of THIS repo (derive them from the link you were given). NEVER run `hermes profile install <repo-root-url>` for this repo: the installable profiles live in `dist/<name>/` subdirectories, so the repo ROOT has NO distribution.yaml and the bare-root install fails — do NOT fall back to downloading/extracting the repo ZIP yourself (that bakes a transient temp-dir `source:` into the profile manifest and breaks `hermes profile update`). The bundled installer scripts always clone to a STABLE cache and install from `./dist/<name>`. After install, open the profile (`hermes -p <name>`) and run `/finish-setup`.",
  "generated_by": "python -m configurator compile",
  "hermes_requires": ">=0.18.0",
  "install": {
    "note": "hermes profile install cannot target a subdirectory of a git repo, and this repo's installable profiles live under dist/<name>/ — so the repo ROOT has no distribution.yaml and a bare `hermes profile install <repo-root-url>` fails. Never improvise a repo-ZIP-to-temp install (it bakes a transient source: that breaks profile update). Install from a LOCAL clone via the bundled installer scripts (NOT a bare hermes profile install, which skips /finish-setup registration on Desktop/default surfaces): from inside a checkout run a profile's local_posix_command / local_windows_command (or bootstrap.* to extend the current profile); standalone, run its standalone_posix_command / standalone_windows_command (which clone the repo to a stable cache for you). See each profile entry.",
    "posix_script": "./install.sh",
    "windows_script": "./install.ps1"
  },
  "profiles": [
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Personal assistant for a software developer — TDD, PR workflow, code review, terminal, on the free chain.",
      "kind": "persona",
      "local_posix_command": "./install.sh developer --yes",
      "local_windows_command": ".\\install.ps1 developer -Yes",
      "name": "developer",
      "path": "dist/developer",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- developer --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p developer -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "General-purpose Hermes agent on a free, no-lock-in model chain.",
      "kind": "base",
      "local_posix_command": "./install.sh general --yes",
      "local_windows_command": ".\\install.ps1 general -Yes",
      "name": "general",
      "path": "dist/general",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- general --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p general -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "pro"
      ],
      "description": "General-purpose Hermes agent on a paid Nous Portal subscription — frontier agentic models plus the Nous Tool Gateway (web/image/TTS/browser), one OAuth login, zero per-tool API keys.",
      "kind": "base",
      "local_posix_command": "./install.sh general-pro --yes",
      "local_windows_command": ".\\install.ps1 general-pro -Yes",
      "name": "general-pro",
      "path": "dist/general-pro",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- general-pro --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p general-pro -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Hebrew-first Israeli agent — RTL-aware, gov/forms-ready, on the free base chain.",
      "kind": "locale",
      "local_posix_command": "./install.sh il --yes",
      "local_windows_command": ".\\install.ps1 il -Yes",
      "name": "il",
      "path": "dist/il",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- il --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p il -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Personal assistant for life in Israel — gov services, consumer rights, forms, budgeting.",
      "kind": "persona",
      "local_posix_command": "./install.sh il-citizen --yes",
      "local_windows_command": ".\\install.ps1 il-citizen -Yes",
      "name": "il-citizen",
      "path": "dist/il-citizen",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- il-citizen --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p il-citizen -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Personal assistant for an Israeli legal professional — contracts, tax, compliance, drafting, with strict disclaimers.",
      "kind": "persona",
      "local_posix_command": "./install.sh il-legal --yes",
      "local_windows_command": ".\\install.ps1 il-legal -Yes",
      "name": "il-legal",
      "path": "dist/il-legal",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- il-legal --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p il-legal -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Personal assistant for an Israeli mental-health therapist — practice management, confidential session notes, scheduling, Hebrew intake forms & letters.",
      "kind": "persona",
      "local_posix_command": "./install.sh il-therapist --yes",
      "local_windows_command": ".\\install.ps1 il-therapist -Yes",
      "name": "il-therapist",
      "path": "dist/il-therapist",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- il-therapist --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p il-therapist -Repo <REPO_GIT_URL> -Yes",
      "version": "2.0.0"
    },
    {
      "apply_modes": [
        "free",
        "pro"
      ],
      "description": "Personal assistant for a US legal professional — jurisdiction-aware research and drafting, with strict disclaimers.",
      "kind": "persona",
      "local_posix_command": "./install.sh us-legal --yes",
      "local_windows_command": ".\\install.ps1 us-legal -Yes",
      "name": "us-legal",
      "path": "dist/us-legal",
      "standalone_posix_command": "curl -fsSL <RAW_REPO_URL>/install.sh | bash -s -- us-legal --repo <REPO_GIT_URL> --yes",
      "standalone_windows_command": "$p=Join-Path $env:TEMP 'hermes-setup-install.ps1'; irm <RAW_REPO_URL>/install.ps1 -OutFile $p; & $p us-legal -Repo <REPO_GIT_URL> -Yes",
      "version": "1.0.0"
    }
  ],
  "schema": "hermes-setup/profiles@1"
}
